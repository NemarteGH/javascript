// Executado com Node.js
// Condições simples

var velocidade = 30
var limite = 60
// Condição simples, executada caso o resultado booleano seja True.
if (velocidade > limite){ 
    console.log(`Você está acima da velocidade permitida de ${limite}.`)
}// Segue o fluxo normal do programa